package no.nav.aura.nare.counters;



public class CounterTestNG {

/*
    @BeforeClass
    public static void setSystemProperties() {
        JGVJettyRunner.setSystemProperties();
    }

    @Test(threadPoolSize = 1000, invocationCount = 1000)
    public void testCount() throws Exception {
        String name = Thread.currentThread().getName();
        String tName = "nr" + name.substring(name.lastIndexOf("-") + 1, name.length());

        int beforeReadValue = Counter.getCount();
        Instant nowInstant = new Instant();
        int i = Counter.plusOne();
        Instant endInstant = new Instant();
        int afterReadValue = Counter.getCount();

        Duration duration = new Duration(nowInstant, endInstant);

        System.out.println("Thread " + tName + " took " + duration + ". It started with value " + beforeReadValue + ", og incremented to " + i + ". Afterwards, the thread read " + afterReadValue + ".");

    }
*/


}